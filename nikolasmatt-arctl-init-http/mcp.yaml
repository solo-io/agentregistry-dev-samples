# Generated by `arctl init mcp`.
# NOTE: spec.source.package.origin.oci.serverName must match the
# image's io.modelcontextprotocol.server.name OCI label. Edit before
# applying against a public registry. (The private-registry exemption
# applies to localhost:* / 127.0.0.1 / [::1].)
#
# For http transport: spec.source.package.transport.port and the --port
# value inside spec.source.package.launch.args reference the same port.
# If you change one, change the other.
apiVersion: ar.dev/v1alpha1
kind: MCPServer
metadata:
  name: nikolasmatt-arctl-init-http
spec:
  description: Sample MCP server (now, add, reverse) deployed from source.
  source:
    package:
      launch:
        args:
          - type: positional
            value: src/main.py
          - type: positional
            value: --transport
          - type: positional
            value: http
          - type: positional
            value: --host
          - type: positional
            value: 0.0.0.0
          - type: positional
            value: --port
          - type: positional
            value: "3000"
        command: python3
      origin:
        identifier: us-docker.pkg.dev/developers-369321/agentregistry-enterprise-public-nonprod/dev-walkthrough/nikolasmatt/arctl-init-http:v2026.6.1-44-g7aee8758
        oci:
          serverName: nikolasmatt-arctl-init-http
        type: oci
      transport:
        path: /mcp
        port: 3000
        type: http
    repository:
      url: https://github.com/solo-io/agentregistry-dev-samples.git
      branch: walkthrough-nikolasmatt
      subfolder: nikolasmatt-arctl-init-http
  title: nikolasmatt-arctl-init-http
