# AUTOGENERATED FILE: DO NOT EDIT
# Generated by the AgentRegistry CLI.

ARG DOCKER_REGISTRY=ghcr.io
ARG VERSION=0.10.1
FROM $DOCKER_REGISTRY/kagent-dev/kagent/kagent-adk:$VERSION

USER root
COPY pyproject.toml /app/pyproject.toml
# Install the agent's dependencies into the base image's Python environment.
RUN --mount=from=ghcr.io/astral-sh/uv:0.12.3,source=/uv,target=/uv \
    ["/uv", "pip", "install", "--python", "/.kagent/.venv/bin/python", "-r", "/app/pyproject.toml"]
USER 65532:65532

WORKDIR /app

COPY xytian315agentdemo/ xytian315agentdemo/
COPY README.md README.md
COPY .python-version .python-version

ENV OTEL_SERVICE_NAME=xytian315agentdemo
ENV PYTHONPATH=/app

CMD ["xytian315agentdemo"]
