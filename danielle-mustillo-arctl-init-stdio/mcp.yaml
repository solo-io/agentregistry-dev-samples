# Generated by `arctl init mcp`.
# NOTE: spec.source.package.origin.oci.serverName must match the
# image's io.modelcontextprotocol.server.name OCI label. Edit before
# applying against a public registry. (The private-registry exemption
# applies to localhost:* / 127.0.0.1 / [::1].)
#
# For http transport: spec.source.package.transport.port and the --port
# value inside spec.source.package.launch.args reference the same port.
# If you change one, change the other.
apiVersion: ar.dev/v1alpha1
kind: MCPServer
metadata:
  name: danielle-mustillo-arctl-init-stdio
spec:
  description: Source-built stdio MCP server (echoes prompts via the fastmcp default echo tool).
  source:
    package:
      launch:
        args:
          - type: positional
            value: src/main.py
          - type: positional
            value: --transport
          - type: positional
            value: stdio
        command: python3
      origin:
        identifier: us-docker.pkg.dev/developers-369321/agentregistry-enterprise-dev/dev-walkthrough/danielle-mustillo/arctl-init-stdio:v2026.8.0-54-gce2de057
        oci:
          serverName: danielle-mustillo-arctl-init-stdio
        type: oci
      transport:
        type: stdio
    repository:
      url: https://github.com/solo-io/agentregistry-dev-samples.git
      branch: walkthrough-danielle-mustillo
      subfolder: danielle-mustillo-arctl-init-stdio
  title: danielle-mustillo-arctl-init-stdio
